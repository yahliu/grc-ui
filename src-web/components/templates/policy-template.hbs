apiVersion: policies.open-cluster-management.io/v1
kind: Policy
metadata:
  name: {{name}}
  namespace: {{namespace}}
  annotations:
    policies.open-cluster-management.io/standards: {{#each standards}}{{ this }}{{^if @last}}, {{/if}}{{/each}}
    policies.open-cluster-management.io/categories: {{#each categories}}{{ this }}{{^if @last}}, {{/if}}{{/each}}
    policies.open-cluster-management.io/controls: {{#each controls}}{{ this }}{{^if @last}}, {{/if}}{{/each}}
spec:
  remediationAction: {{#if enforce}}enforce{{else}}inform{{/if}}
  disabled: {{#if disabled}}true{{else}}false{{/if}}
  {{! If user modifies one of these specs, editor captures those changes and will insert here on re-render}}
  {{#if specsCapture}}
{{{specsCapture}}}
  {{else}}
  {{#if policyTemplates.length}}
  policy-templates:
    {{#each policyTemplates}}
    {{{ this }}}
    {{/each}}
  {{/if}}
  {{/if}}
---
apiVersion: policies.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: binding-{{name}}
  namespace: {{namespace}}
placementRef:
  name: placement-{{name}}
  kind: PlacementRule
  apiGroup: apps.open-cluster-management.io
subjects:
- name: {{name}}
  kind: Policy
  apiGroup: policies.open-cluster-management.io
---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-{{name}}
  namespace: {{namespace}}
spec:
  clusterConditions:
  - type: OK
  clusterSelector:
    matchExpressions:
      {{#each clusters}}
      - {key: {{@key}}, operator: In, values: [{{#each this}}{{#if @index}}, {{/if}}"{{this}}"{{/each}}]}
      {{else}}
      []
      {{/each}}
