apiVersion: policies.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-create-issuer-[TIME]
  namespace: default
  annotations:
    policies.open-cluster-management.io/categories: PR.PT Protective Technology
    policies.open-cluster-management.io/controls: PR.PT-3 Least Functionality
    policies.open-cluster-management.io/standards: NIST-CSF
spec:
  complianceType: musthave
  disabled: false
  namespaces:
    exclude:
      - kube-*
    include:
      - default
  object-templates:
    - complianceType: musthave
      objectDefinition:
        apiVersion: cert-manager.io/v1alpha3
        kind: Issuer
        metadata:
          name: ss-sample-issuer-[TIME]
          namespace: default
        spec:
          selfSigned: {}
  remediationAction: enforce
---
apiVersion: policies.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: binding-policy-create-issuer-[TIME]
  namespace: default
placementRef:
  name: placement-policy-create-issuer-[TIME]
  kind: PlacementRule
  apiGroup: apps.open-cluster-management.io
subjects:
- name: policy-create-issuer-[TIME]
  kind: Policy
  apiGroup: policies.open-cluster-management.io
---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-policy-create-issuer-[TIME]
  namespace: default
spec:
  clusterConditions:
  - type: OK
  clusterSelector:
    matchExpressions:
      []
